{
  "name": "thrive-backend",
  "version": "1.0.0",
  "description": "Thrive sync API — daily deals, coupons, recipes, and catalog feeds with ETag caching and pluggable retailer sources.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/backend.test.js test/location.test.js test/google.test.js test/security.test.js && node test_kroger.js && node test_target.js && node test_exa.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=22.13"
  }
}
